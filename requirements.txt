# Giant Creative Ad Engine — runtime dependencies
#
# Layer 1 (signal) and the deterministic pipeline (diagnosis, hypothesis) need only
# pandas/numpy. The agent and app layers add their own deps as they are implemented.
pandas>=2.0
numpy>=1.24

# Layer 4 (agent) — Gemini, per the project's provider choice (the spec/CLAUDE.md
# note Anthropic; this implementation uses google-genai with gemini-2.5-flash and
# reads the key from GEMINI_API_KEY / GOOGLE_API_KEY).
google-genai>=1.0

# Layer 5 (app) — Streamlit dashboard + chat UI (run: streamlit run app/app.py)
streamlit>=1.30
